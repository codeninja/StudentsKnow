<h1>Editing reply</h1>

<%= error_messages_for :comment %>

<% form_for :comment, :url => admin_edit_reply_path do |f| %>
<p><label for="topic">Topic</label>
<%= f.text_field :title %></p>

<p><label for="data">Your thoughts </label>
<%= f.text_area :comment, :cols => 50, :rows => 10  %></p>


  <p>
    <%= submit_tag "Update" %>
    <%= link_to 'Back', admin_replies_list_path(:id => @message.id) %>
  </p>

  
  <% end %>

