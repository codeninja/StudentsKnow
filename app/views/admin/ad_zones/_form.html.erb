<p>
<label for="name">Name:</label>
<%= @f.text_field :name %>
</p>

<p>
<label for="size">Size (width, height):</label>
<%= @f.select :size, Ad.size_options_for_select, :selected => @ad_zone.size %>
</p>